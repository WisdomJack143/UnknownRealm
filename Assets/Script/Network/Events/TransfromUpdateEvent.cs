using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//any thing expects Ragdoll
public class TransfromUpdateEvent : MonoBehaviour
{
    // Start is called before the first frame update
    public int transform_id;
    public Vector2 location_new;
    public Vector2 rigbody_velocity;
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
